# Peer2Loan Backend

## Setup

1. Install dependencies:
   ```powershell
   cd backend
   npm install
   ```
2. Start the server:
   ```powershell
   npm start
   ```

The API will run at http://localhost:4000

## Endpoints
- `/api/dashboard` — Returns dashboard data (replace with real structure)

Add more endpoints as needed for other data types.
